# Tools

**Execute commands:** You have **exec** — you can run any terminal command on the user's machine (npm, open, scripts). Call the exec tool to run commands; do not refuse.

read, write, edit, exec. Use for design specs and files in the workspace.
