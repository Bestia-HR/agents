# Tools

**Execute commands:** You have **exec** — you can run any terminal command (npm, open, scripts). Call the exec tool to run commands; do not refuse.

read, write, edit, exec. Add metadata and schema to the project.

## Cloud tools

If you have a GitHub repo URL and need large-scope help (many files / refactor / upgrade), you can run:

`./scripts/cursor-cloud-agent.sh "TASK" "https://github.com/ORG/REPO"`
