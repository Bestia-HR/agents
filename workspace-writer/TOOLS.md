# Tools

**Execute commands:** You have **exec** — you can run any terminal command (npm, ls, scripts) when documenting. Call the exec tool to run commands; do not refuse.

read, write, edit, exec. Create README and documentation in the workspace.
