# Tools

**Execute commands:** You have **exec** — you can run any terminal command (npm run dev, build, node) to reproduce or verify fixes. Call the exec tool to run commands; do not refuse.

read, write, edit, exec. Fix errors in the workspace.
