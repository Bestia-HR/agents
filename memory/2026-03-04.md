# Durable memory — 2026-03-04

## OpenClaw + Telegram (Macbotd)

- **User Telegram ID:** 8455470574 (in USER.md and openclaw.json allowFrom / groupAllowFrom).
- **Macbotd group ID:** -5049131940.
- **Agents:** Main (Agent A, account main), AdminCreateWebsiteBot (Agent B, account admin). Both use local Ollama as primary.
- **Bots not seeing messages:** Disable Telegram privacy for both bots via @BotFather → `/setprivacy` → select each bot → Disable. OpenClaw config (requireMention: false, groupAllowFrom) is already set.

## Ollama local

- **Primary model:** qwen2.5-coder:7b (ollama/qwen2.5-coder:7b). Switched from llama3.2.
- **Ollama baseUrl:** http://127.0.0.1:11434.
- **OpenClaw 2026.3.2:** Does not support `connectTimeoutMs` or `requestTimeoutMs` under models.providers.ollama. Adding them causes "Unrecognized keys"; remove with `openclaw doctor --fix` if present.
- **Avoid "LLM request timed out":** (1) Pre-load model: `./warm-ollama.sh` or `ollama run qwen2.5-coder:7b` with OLLAMA_KEEP_ALIVE=-1. (2) Keep maxTokens at 4096 so replies finish before internal timeout. (3) run-gateway.sh warms the model on start when Ollama is reachable.

## Config choices

- **Agent run timeout:** agents.defaults.timeoutSeconds = 315360000 (10 years).
- **Voice/TTS off:** messages.tts.auto "off", tools.deny includes "tts".
- **Sessions:** ./clear-sessions.sh clears all agent sessions; restart gateway after.
- **Docs:** START-HERE.md, docs/OLLAMA-NOT-RESPONDING-IN-TELEGRAM.md, docs/BOT-NOT-ANSWERING-HI.md, docs/LLM-REQUEST-TIMED-OUT.md, docs/WHY-LOCAL-LLM-SLOW.md.

## Scripts

- **./run-gateway.sh** — start gateway; warms qwen2.5-coder:7b if Ollama up.
- **./warm-ollama.sh** — keep qwen2.5-coder:7b loaded (run in separate terminal).
- **./check-telegram-ollama.sh** — verify Ollama and gateway reachable, model available.
- **./clear-sessions.sh** — clear all OpenClaw sessions (stops gateway first).
