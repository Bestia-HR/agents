# Tools

**Execute commands:** You have **exec** — you can run any terminal command (npx prisma, npm, scripts). Call the exec tool to run commands; do not refuse.

read, write, edit, exec. Create Prisma schema and migration files in the workspace.

## Cloud tools

If you have a GitHub repo URL and need large-scope help (many files / refactor / upgrade), you can run:

`./scripts/cursor-cloud-agent.sh "TASK" "https://github.com/ORG/REPO"`
